apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mailu-cert-sync
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/Pavliin/MyOwn.git
      targetRevision: v0.1.13
      path: gitops/manifests/mailu-cert-sync
  destination:
    server: https://kubernetes.default.svc
    # Nominal only — every resource in this Application sets its own
    # explicit namespace (kube-system or mailu), see kustomization.yaml.
    namespace: kube-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
