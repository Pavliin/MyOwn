apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-restic-repo
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
