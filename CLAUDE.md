# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repository is

A personal self-hosted cloud project (files, photos, passwords, messaging, mail — replacing GAFAM services), starting as a solo POC for the author's family/friends. Full context lives in `docs/`, read it before making architectural decisions:

- [`docs/architecture.md`](docs/architecture.md) — chosen stack, infra, security, backup, known risks
- [`docs/materiel.md`](docs/materiel.md) — mini PC sizing/purchase research and decisions (two-stage plan, real pricing, rejected options)
- [`docs/installation-utilisateur.md`](docs/installation-utilisateur.md) — Phase 6 installer design: script approach, admin-account model (Authentik + Vaultwarden-as-vault, no residual operator access)
- [`docs/roadmap.md`](docs/roadmap.md) — phased development plan with exit criteria per phase
- [`docs/pitch.md`](docs/pitch.md) — technical presentation of the project
- [`docs/vision-long-terme.md`](docs/vision-long-terme.md) — long-term directions to keep in mind, out of scope for the POC
- [`docs/notes-techniques.md`](docs/notes-techniques.md) — as-built reference: deployed versions, notable config choices, gotchas actually hit and how they were resolved
- [`docs/manuel-installation.md`](docs/manuel-installation.md) — reproducible steps for a first install
- [`docs/manuel-utilisateur.md`](docs/manuel-utilisateur.md) — how to use each deployed service (URL, credentials, basics)

**Keep the last three updated as you go, not retroactively**: whenever a service is added or changed in `gitops/apps/`, or a real bug/gotcha is hit and fixed (not a typo — an actual "this didn't work the way the docs/chart suggested" moment), add it to `notes-techniques.md` in the same PR. Same for `manuel-installation.md` (new bootstrap step) and `manuel-utilisateur.md` (new service reachable by a human) whenever they change. Retrofitting these later is much easier to get wrong or skip entirely.

## Current state

**Read this section first in any new conversation** — it's the single pointer to where the project actually stands, kept current at each natural completion point (end of a phase, end of an integration) rather than after every commit. For the full detail behind any of this, see `docs/notes-techniques.md` (as-built) and `docs/roadmap.md` (phase plan) — don't re-derive from `git log` unless this section seems stale.

Local k3d dev cluster (`myown-dev`) only — no mini PC or domain yet. Two access patterns: most services on `myown-<service>.local:8090` (HTTP); Vaultwarden, Nextcloud, and Tuwunel on `:8453` (HTTPS, mkcert) — each for a different reason: Vaultwarden needs the browser's Subtle Crypto API, Nextcloud's `user_oidc` SSO app flatly refuses to run over HTTP, Tuwunel's SSO grant-session cookie is `Secure`-flagged and browsers drop it over HTTP. ArgoCD (`gitops/apps/`, app-of-apps) manages everything except the bootstrap layer itself (`gitops/bootstrap/`: ArgoCD, KSOPS, internal DNS/Traefik-mirror — see `manuel-installation.md`).

- **Roadmap restructured (2026-08-08)**: everything that hard-requires the mini PC/domain (Android app validation across services, Matrix federation test) was pulled out of Phases 1-3 and merged into Phase 4, now "Bascule vers l'infra réelle & Mail" — Mail needed that infra from day one anyway, so it's a single hardware/domain cutover instead of one per phase. Phases 1-3 are now fully closable on the dev cluster alone. See `docs/roadmap.md` for the exact split.
- **Roadmap Phase 0 ("Socle")**: complete. k3s(dev)/ArgoCD/monitoring/Authentik/secrets(SOPS+KSOPS) all working.
- **Roadmap Phase 1 ("Vaultwarden")**: complete — Vaultwarden deployed and SSO-integrated with Authentik (via an Authentik blueprint, not the admin UI; took 4 real bugs to get a login to actually succeed, all in `notes-techniques.md`). Restic backup pipeline done and validated (daily CronJob, backup + full restore tested in the dev cluster — targets a local PVC as a stand-in for the real "nœud ami", which doesn't exist yet; see `notes-techniques.md`). Android app validation is no longer part of this phase's exit criteria — deferred to Phase 4.
- **Roadmap Phase 2 ("Nextcloud + Immich")**: both services deployed and working. Nextcloud: SSO-integrated with Authentik and validated end-to-end (official `user_oidc` app via `hooks.before-starting`; HTTPS on `:8453`; also fixed an SSRF guard and a Redis-session password encoding bug — see `notes-techniques.md`), Calendar/Contacts installed, Restic backup extended (files + `pg_dump` of Postgres) and validated with a real backup+restore test, native Photos app disabled now that Immich exists (one photo app per architecture.md §2). Immich: deployed (official `immich/immich` chart; Postgres is a hand-written StatefulSet on `ghcr.io/immich-app/postgres`, incompatible with the Bitnami postgresql chart used elsewhere) and validated end-to-end via the API (account creation, login, upload, confirmed on the library PVC). SSO integrated too and validated with a real browser login — Immich has native OAuth support (no third-party app like Nextcloud's `user_oidc`), configured via a partial `IMMICH_CONFIG_FILE` (merges with schema defaults, confirmed in the server source — only the `oauth` section is set, nothing else touched) rather than the Admin Settings UI, on the user's explicit ask that no one should ever have to click through manual config. Restic backup extended too and validated with a real backup+restore test (library PVC + `pg_dump` of Postgres, same shape as Nextcloud's — ML cache PVC deliberately excluded, fully reproducible). Still open for Immich: Android app (deferred to Phase 4 by roadmap design).
- **Roadmap Phase 3 ("Messagerie") — started (2026-08-09)**: first service, **Tuwunel**, deployed bare and validated on the dev cluster — not Conduwuit as originally planned in `architecture.md`, discovered archived/dead upstream while preparing this deployment (not viable for storing private family messages long-term). **Tuwunel** picked over Continuwuity (community fork, no confirmed native SSO): official successor, government-scale adoption (Switzerland), staffed full-time, native OIDC support already merged — fits this project's all-Authentik pattern. No up-to-date Helm chart for either successor → raw manifests (`gitops/manifests/tuwunel/`: `Deployment` + `Service` + `PVC` + `Ingress`), dedicated ArgoCD `Application` (`gitops/apps/tuwunel.yaml`). Embedded RocksDB (no separate Postgres, like Vaultwarden). Client-server port `8008`; federation (`8448`) deferred to Phase 4. Two gotchas from the bare deploy (full detail in `notes-techniques.md`): the naming collision between the `tuwunel` Service and the app's own `TUWUNEL_*` env prefix (fixed with `enableServiceLinks: false`), and the default config binding to loopback only (fixed with an explicit `address`). **Validated end-to-end via the client-server API**: registration, first account auto-joined the server's admin room, test message sent successfully.
- **Tuwunel ↔ Authentik SSO — wired and validated (2026-08-09)**: config moved entirely from `TUWUNEL_*` env vars to one mounted `tuwunel.toml` (`gitops/secrets/tuwunel/`) — the `[[global.identity_provider]]` block SSO needs can't be set via env vars at all (broken upstream, tuwunel#340), and consolidating everything into one file is simpler for a from-scratch install than splitting config across two places. Authentik blueprint follows the established pattern (`gitops/secrets/authentik-blueprints/tuwunel-sso.sops.yaml`), reusing the shared "Vaultwarden: email (verified)" scope mapping. Includes `userid_claims = ["preferred_username"]`, a gotcha confirmed by another operator's real Authentik+Tuwunel setup (tuwunel#249). **Real bug found via an actual browser login attempt** (curl alone didn't catch it): Tuwunel's `tuwunel_grant_session` OAuth cookie is `Secure`-flagged, silently dropped by the browser over plain HTTP → `Missing cookie` at the callback step. Fixed by moving Tuwunel to HTTPS (`:8453`, mkcert, `tuwunel-tls` secret) — third service to land there, third distinct reason (Subtle Crypto for Vaultwarden, `user_oidc` for Nextcloud, this cookie for Tuwunel). Also hit the same "ArgoCD selfHeal fights manual pre-merge testing" friction already documented for Vaultwarden's SSO, and the same "blueprint needs `ak apply_blueprint` triggered manually" gotcha. **Confirmed working end-to-end after the fix**: real browser login via Authentik, `loginToken` issued, exchanged for a real session (`user_id: "@akadmin:myown-tuwunel.local"` — `userid_claims` correctly mapped the Authentik username, not a generated ID). Full write-up in `notes-techniques.md`.
- **Tuwunel Restic backup — done and validated (2026-08-09)**: exact copy of Vaultwarden's pattern (`gitops/manifests/tuwunel-backup/`, 3rd source on the `tuwunel` Application, `RESTIC_PASSWORD` added to the existing `tuwunel-secrets` via `sops --set`) — no `pg_dump` needed, RocksDB is embedded like Vaultwarden's SQLite. Scheduled `03:45`, after the other three. Validated with a real backup + restore into a disposable pod: 72/73 files matched exactly; the one difference (the active WAL log file) is expected — confirmed via size/mtime that it's just ongoing writes between snapshot and diff, not corruption (RocksDB's WAL format is designed to tolerate hot copies). Full write-up in `notes-techniques.md`.
- **LiveKit — deployed and validated end-to-end (2026-08-10)**: last piece of Phase 3's declared scope. Official `livekit/livekit-server` chart (single-port RTC mode, `podHostNetwork: true` for correct STUN-discovered external IP) plus `lk-jwt-service` (raw manifests, `ghcr.io/element-hq/lk-jwt-service`, issues LiveKit JWTs after verifying a Matrix OpenID token against Tuwunel). Required recreating the dev cluster twice to add new k3d port mappings (`7880`/`7881`/`7882` for LiveKit's RTC ports, then `443` — MatrixRTC discovery needs `.well-known/matrix/{client,server}` on the *standard* port, not this project's usual `:8453`); both recreations were preceded by a fresh Restic backup + `kubectl cp` extraction of all 4 stateful services' repos to the host disk first, since those repos themselves live on in-cluster PVCs and would otherwise have been lost along with the cluster (first real test of restoring onto a brand-new cluster, not just the same one). Wired into Tuwunel via `[global.well_known]` in `tuwunel.toml` (`livekit_url`, publishes `org.matrix.msc4143.rtc_foci`). Multiple real bugs found only via an actual Element Call browser join (curl never caught them): a `Service` named `livekit` colliding with Kubernetes' auto-injected `LIVEKIT_PORT` env var (same class as Tuwunel's `enableServiceLinks` gotcha); `lk-jwt-service`'s well-known lookup to Tuwunel needing `SSL_CERT_FILE` specifically, since `gomatrixserverlib/fclient`'s `LookupWellKnown()` ignores `LIVEKIT_INSECURE_SKIP_VERIFY_TLS` (that flag only covers the separate federation-request transport); `LIVEKIT_URL` needing to be the public `wss://` hostname, not internal cluster DNS, since it's echoed back to the browser as the actual media-connection target. Full write-up including a periodic ~15s reconnect loop (root cause: the chart's default server image, `v1.9.0`/protocol 16, doesn't support the `/rtc/v1` signaling path the JS client tries first — fixed by pinning `image.tag: v1.13.5`, independent of the chart version) in `notes-techniques.md`. **Validated end-to-end in a real browser**: Element Call join, published audio, real UDP media, working reactions, no reconnect loop after the version fix. **Not yet done**: a real second-device LAN test (only single-machine browser testing so far) — the specific point of uncertainty flagged before starting this work (whether LiveKit announces a usable external IP to a genuinely separate peer, not just two tabs on the same machine).
- **Authentik Restic backup — done and validated (2026-08-13)**: closes a gap open since Phase 0 — Authentik was the only service with no backup, losing its admin account and SSO config on every cluster recreation (worked around manually via `ak shell` during the LiveKit work). Different shape from the other four: confirmed live that `authentik-server`/`authentik-worker` have no PVC of their own (only the Postgres `StatefulSet` does) — everything (users, flows, providers, blueprint-applied state) lives in Postgres, blueprints themselves already reproducible from git. So this is `pg_dump`-only, no files PVC to mount alongside it. Scheduled `04:00`, after the other four. Validated with a real backup + restore into a disposable pod: dump restored intact (215 tables, `akadmin` account with password hash and event history present).
- **Roadmap Phase 3.5 added (2026-08-13)**: a planning session (mini PC sizing, Phase 6 installer/admin-account design, IA scope, ops) surfaced a list of work that needs neither the mini PC nor the domain — inserted as a new phase between 3 and 4 so it isn't lost, see `roadmap.md`. First item done same day: **Uptime Kuma monitors + public status page**, configured via a new script (`scripts/uptime-kuma-setup.py`) rather than the UI — chosen after browser tool access to the local Uptime Kuma origin hit an unresolvable per-site approval gate. Six family-facing services monitored (Authentik, Vaultwarden, Nextcloud, Immich, Tuwunel, LiveKit; ArgoCD/Grafana deliberately excluded, admin-only), published at `/status/etat-du-systeme`. Uptime Kuma 2.3.0 turned out to be meaningfully ahead of the `uptime-kuma-api` client library (1.2.1, latest on PyPI): needed a raw HTTP call for a first-boot `/setup-database` step the library doesn't know about, plus two schema-drift workarounds (a `conditions` NOT NULL column the library omits on monitor creation, and an `incident`→`incidents` rename that breaks the library's status-page save path) — all three documented with the exact fix in `notes-techniques.md`. Also found and documented (but left unfixed, out of scope) a real Authentik bug unrelated to monitoring: anonymous requests to `/` redirect to `/setup`, which 500s with `FlowNonApplicableException` once an admin account already exists — worked around by pointing the monitor at `/-/health/live/` instead, the correct target anyway.
- **Tuwunel alert bot — done and validated (2026-08-14)**: second Phase 3.5 item. `@alertbot:myown-tuwunel.local` (registered via a new `scripts/tuwunel-alertbot-setup.py`, same UIAA flow as any user account) owns a shared, joinable `#etat-du-systeme:myown-tuwunel.local` room that Uptime Kuma posts to via its native Matrix notification provider — no custom webhook bot needed. Deliberately separate from the private per-user DMs Ollama will use in Phase 5 for AI proposals (shared infra alerts vs. personal automation, never mixed). Hit the same CA-trust gotcha as `lk-jwt-service` a second time, this time in Node.js: Uptime Kuma's outbound HTTPS call to Tuwunel failed silently in the UI (`unable to verify the first certificate`) until `NODE_EXTRA_CA_CERTS` was wired to a `mkcert-ca` ConfigMap (`gitops/apps/uptime-kuma.yaml`, one ConfigMap per namespace, not shareable with `livekit`'s). Also re-hit the known "root's selfHeal reverts a live-tested fix that isn't committed yet" friction — same `syncPolicy: null` workaround as before. **Validated end-to-end**: a real forced down/up cycle on the LiveKit monitor produced an actual `[LiveKit] [✅ Up] 200 - OK` message from `alertbot`, confirmed by reading the room via the Matrix API rather than assumed from config alone.
- **Ollama deployed, Qwen3 8B chosen (2026-08-14)**: third Phase 3.5 item. Community chart `otwld/ollama-helm` (1.74.0), declarative model pull. Ran a real comparative test — Qwen3 8B vs. Mistral 7B vs. Llama 3.1 8B — on French/English/Russian mail summarization plus structured event/deadline extraction (the actual Phase 5 need, not just prose). Two real methodology bugs found and fixed before the result was trustworthy: the first pass had all three models answering in French regardless of the email's language (the prompt instruction itself was in French, biasing every model — fixed with per-language prompts); the extraction pass initially never told the model what "today" was, making ISO date computation from "mardi 18 août" impossible — fixed by adding the current date to the prompt. Also found Qwen3 runs an extensive hidden "thinking" trace by default even for trivial prompts (confirmed via the `thinking` field in a raw API response), explaining an apparent 40-130s slowdown; `"think": false` in the request body fixed it, dropping Qwen3 to the fastest of the three. Final result: Qwen3 8B correct on 9/12 extraction fields (only remaining misses are a benign "defaults to today's date instead of null" schema quirk — action/deadline detection correct in every case), vs. 6/12 for Mistral and 5/12 for Llama 3.1 (which also got a real comprehension question wrong — said no action required for an appointment explicitly requiring 24h cancellation notice, and doesn't officially support Russian at all). User chose Qwen3 8B knowingly despite a stated preference for a French model (Mistral), given the clear measured gap. Also hit a real OOM (exit 137, confirmed via `kubectl describe pod`) mid-test — Ollama kept the prior model resident while loading the next one, exceeding the pod's memory limit; fixed with `OLLAMA_MAX_LOADED_MODELS: "1"`, also the right behavior for Phase 5 regardless. Mistral/Llama 3.1 removed from the pod after the test to reclaim disk (~9Gi) — only `qwen3:8b` remains pulled. Full methodology and results in `notes-techniques.md`.
- **Full mail → calendar pipeline prototyped and validated end-to-end (2026-08-14)**: closes out the Ollama/Phase 3.5 mail track. Needed a disposable test mailbox (creating an account with a real provider is out of scope for the assistant per `CLAUDE.md`'s security rules, and the user preferred a self-hosted throwaway anyway) — first tried Mailpit, discovered it only supports SMTP + a web UI/API, **no IMAP**, useless for testing an IMAP connector specifically. Replaced with **GreenMail** (`greenmail/standalone`), purpose-built for this (SMTP+IMAP+POP3 test server, no real delivery), deployed outside GitOps as disposable test scaffolding, same treatment as the model-comparison scripts. Full chain validated with every step independently checked rather than assumed: 3 test emails injected via real SMTP → fetched via real IMAP (`imaplib`, not a REST shortcut) → extracted via Qwen3 (same accuracy as the earlier comparative test) → proposal sent as a private Matrix DM from `@alertbot` (same bot as admin alerting, reused in DM mode — never mixed with the shared alert room, per the design already agreed) → **real human confirmation**: the user joined via a `matrix.to` link (room-search-by-alias still not reliable in Element Web, same finding as before) and replied "oui" from their own client → reply detected via polling → event written to Nextcloud Calendar via CalDAV (`PROPFIND`-confirmed default calendar, not guessed) only after confirmation, never automatically → re-read directly from Nextcloud to confirm real persistence, not just a `201` status. Left out of this prototype on purpose (real Phase 5 scope): switching to actual Mailcow, replacing polling with real Matrix `/sync`, recurring scheduling, error-case handling. **Two further real bugs found only by checking the actual Nextcloud Calendar UI** (not the API — the most thorough API check done in this project, a `REPORT` with a `time-range` filter matching the app's own query, still wasn't enough on its own): a floating-time `DTSTART`/`DTEND` (no `Z`, no `TZID`) stores and queries fine but never renders in the UI, fixed by writing explicit UTC times; and the event initially landed on the local `admin` Nextcloud account while the user browses via Authentik SSO, which `user_oidc` provisions as a **separate account** under an opaque hash-based username (confirmed via `occ user:info`, `backend: user_oidc`) — that account also flatly refuses local-password auth by design, fixed with an app-specific token (`occ user:auth-tokens:add`). Real implication flagged for Phase 5: the connector needs to resolve each user's actual Nextcloud account id, not assume a username.
- **Admin-account model prototyped and validated (2026-08-15)**: last Phase 3.5 item on the installer-design track. Confirmed the mechanism `installation-utilisateur.md` had flagged as unvalidated — pushing generated secrets into a user's Vaultwarden vault via the official `bw` CLI right after their first login — actually works. Installed `bw` locally (not globally, avoiding root) against a disposable test Vaultwarden account the user created by hand (master-password creation is the one genuinely unavoidable human step, by zero-knowledge design — no shortcut attempted). Created both a secure-note and a full login-type item (username/password/URI, the real shape for ArgoCD/Grafana credentials) successfully. Verified the actual security property, not just that the call succeeded: queried Vaultwarden's raw API directly (bypassing `bw`'s client-side decryption) and confirmed the server only ever stores proper Bitwarden `CipherString` ciphertext — no plaintext password visible anywhere server-side. Test items deleted and session logged out after validation.
- **ArgoCD sync philosophy clarified, announcement channel built (2026-08-15)**: surfaced by the user questioning whether auto-syncing every `git push` straight to family-facing services fits the project's "you stay in control" ethos. Clarified two things explicitly: ArgoCD is not the orchestrator (k3s is — every service keeps running exactly as-is if ArgoCD were removed entirely, it only reconciles cluster state with git) and each MyOwn deployment has its own local ArgoCD instance, no central trigger should ever exist that syncs someone else's installation (consistent with `vision-long-terme.md`'s no-central-operator principle). Decided (not yet implemented — deferred until real family usage exists): sensitive family-facing services (Authentik, Vaultwarden, Nextcloud, Immich, Tuwunel, LiveKit, Ollama) will eventually move to manual sync, triggered locally by whoever administers that specific installation, preceded by an announcement; infra services invisible to the family (`monitoring`, `uptime-kuma`, `root` itself) stay automatic. Built and validated today: `scripts/tuwunel-announce.py`, reusing the existing alertbot account and `#etat-du-systeme` room rather than a new channel (same "what's going on with our system?" concern from the family's perspective) — tested with a real message posted and read back from the room, not just assumed to have sent.
- **Installer script drafted, two real bugs found across two test attempts (2026-08-15)**: `scripts/install.sh` automates `manuel-installation.md` end to end (tool checks, age key, k3d cluster, ArgoCD, KSOPS, health-check patch, GitOps bootstrap, mkcert certs, `/etc/hosts`), parameterized via env vars so it can target a throwaway cluster instead of the real `myown-dev`. **First attempt** (test cluster alongside the still-running dev cluster): resource contention from two simultaneous k3d/k3s/containerd stacks starved the test cluster's containerd — node never registered. Not a script flaw; test cluster torn down, real cluster confirmed untouched. **Second attempt** (user's idea: `k3d cluster stop myown-dev` first, freeing ports and resources, then test with real default ports): got much further — cluster created, ArgoCD available, KSOPS patch applied, GitOps bootstrap applied, `root` Application synced and correctly auto-discovered all 9 child Applications matching the real cluster. Found two real bugs: (1) **confirmed and fixed** — the KSOPS step patched `argocd-repo-server` with no rollout-status wait (unlike the `argocd-server` step, which has one), so ArgoCD's controller compared manifests against the old pre-KSOPS pod and cached a `ComparisonError: ksops: executable file not found in $PATH` on `authentik` (probably others) even though the init-container was present and the rollout completed moments later — a hard-refresh did not clear the cached error, confirming the race; fixed by adding `kubectl rollout status deployment argocd-repo-server -n argocd --timeout=120s` right after the patch. (2) **real disk-pressure constraint, not a script bug** — `k3d cluster stop` frees ports but not disk: `myown-dev`'s cached images stayed on disk while the test cluster pulled a second full image set (ArgoCD + 9 services, including Ollama's 5.2Gi `qwen3:8b`), pushing this dev machine's already-90%-full root partition (18G free of 184G) into real kubelet `DiskPressure: True`, cascading into evictions across nearly every ArgoCD pod. Resolved by deleting the test cluster (~24Gi reclaimed) and restarting `myown-dev` normally (own cached images, no new pulls) — confirmed healthy, all 10 Applications `Synced`/`Healthy`. Full write-up in `notes-techniques.md`. **Not yet done**: full end-to-end validation past the KSOPS step (mkcert, hosts, final verification) — blocked on finding a test strategy that doesn't require a second full image set on this machine's current disk budget (image pruning between attempts, or a machine with more headroom).
- **WireGuard admin VPN — deployed and validated end-to-end on the LAN (2026-08-15)**: last Phase 3.5 backlog item. Deliberately host-level systemd, not a k8s `Deployment` — this VPN gates access to the cluster (ArgoCD, kubectl, SSH), so putting it inside the cluster it's meant to rescue would create a circular dependency if the cluster itself is down (full reasoning in `wireguard/README.md`). `scripts/wireguard-setup.sh` bootstraps the server and adds peers on later runs, needs interactive `sudo` so it had to be run by the user directly (confirmed non-interactive `sudo` isn't available in this environment). **Validated by the user's real run**: `wireguard-tools` installed, server + first peer ("admin") keys generated, `wireguard/wg0.conf.sops.yaml` committed encrypted and round-trip-verified via `sops -d`, `wg-quick@wg0` active, `wg0` interface up with the correct address. **Real client handshake validated too**: client config imported into the mobile WireGuard app via QR code (had to use the app's own QR scanner, not the phone's regular camera — a camera-app scan failed with "this app was not found on your device", the phone trying to interpret the content as a link instead of handing it to the WireGuard app). Tunnel activated from a real second device (phone) on the LAN: `sudo wg show` confirmed a real handshake with real bidirectional traffic — full functional validation, not just structural. **Not yet done**: the real outside-the-LAN test (needs a real router port-forward, deferred to Phase 4 along with routing into the cluster's internal admin endpoints once a real firewall exists).

- **Nextcloud Notes and Tasks apps added (2026-08-15)**: not tied to a specific roadmap phase, requested directly by the user after confirming they cover distinct, non-redundant needs — Notes for freeform shared text (e.g. a gift list visible to the whole family), Tasks for CalDAV-based shared checklists (e.g. a shopping list kept in sync with a partner in real time, with due-date tasks also surfacing as checkable entries directly in Calendar). Installed the same way Calendar/Contacts already were — `php occ app:install <id> || php occ app:enable <id>` in the `before-starting` hook (`gitops/apps/nextcloud.yaml`), idempotent and safe to re-run on every container start.

- **Roadmap Phase 4 started — mini PC received and bootstrapped on real k3s bare-metal (2026-08-20)**: Dell OptiPlex 5060 (`materiel.md`) arrived and set up — Ubuntu Desktop 26.04 (chosen over Server for comfortable hands-on access during initial setup), fixed via a real UEFI/Legacy boot mismatch (`boot-repair`, full reformat rather than a surgical fix — disk had no data yet), DHCP reservation on the Freebox, SSH key-based access from the dev machine (dedicated key, passwordless `sudo` explicitly granted by the user for this dedicated server). Piloted entirely over SSH from the dev machine's Claude Code session per the user's explicit choice (offered against installing Claude Code locally on the mini PC) — see `docs/manuel-installation.md` section 14 for the resulting bare-metal procedure. **First real k3d → k3s bare-metal migration**: confirmed k3s ships its own Traefik (no port-mapping trick needed like k3d, literal port 443 covered natively) — new `gitops/bootstrap/traefik-external-svc.yaml` (LoadBalancer mirror of the existing `traefik-internal-svc.yaml` pattern) covers the project's 8090/8453 scheme via k3s's built-in ServiceLB. ArgoCD/KSOPS/GitOps bootstrap replayed identically; all 10 Applications auto-discovered and, after real debugging, genuinely working (verified via direct HTTP checks per service, not just ArgoCD's own status — the 6 `CronJob`-bearing Applications stayed `Progressing` on this freshly-created cluster because their Restic-repo PVC only binds on the CronJob's first scheduled run, still hours away; root cause confirmed via the ArgoCD resource-tree API, not assumed — see `notes-techniques.md`). **Multi-layered real bug chased down on Nextcloud** (full chain in `notes-techniques.md`): a `hostPath` directory that must exist with correct `www-data` group ownership *before* the very first boot (missing on a genuinely fresh node) cascaded into a failed auto-install, a stale 0-byte `config.php` that permanently blocked the image's own retry logic, a liveness probe aggressive enough to kill the container mid-install, and a `before-starting` hook (Calendar/Contacts/Notes/Tasks/`user_oidc`) whose "safe, idempotent on every start" assumption had never actually been tested against a truly fresh install — fixed at the source with an install-state guard. Also fixed a real latent bug in `scripts/install.sh`'s mkcert loop (wrong namespace derived for `myown-livekit-jwt.local`). **Not yet done**: the `nextcloud`/`root` Applications are still running with `syncPolicy: null` (disabled live to test the fix pre-merge, same friction already known from Uptime Kuma — this time confirmed nested through the app-of-apps pattern) — re-enable once this fix is merged.

- **Dev/prod separation clarified: `master` stays trunk-based, prod tracks a release tag instead (2026-08-20)**: raised by the user directly — how to keep iterating on `master` without instantly affecting the family-facing mini PC, without reviving a permanent `mainDev` branch (would undo the trunk-based decision `CLAUDE.md` already made). Landed on reusing machinery the project already has rather than inventing new process: dev's `root` Application keeps tracking `targetRevision: master` (fast, disposable), the mini PC's `root` now tracks a release tag instead — "promotion" is cutting a release once validated on dev, same manual-sync-plus-announcement gate already decided 2026-08-15 for sensitive services. **Real gap found making this concrete**: pinning only `root`'s own `targetRevision` doesn't cascade — every child `Application` (`gitops/apps/*.yaml`) has its own independent `repoURL: .../MyOwn.git` source for secrets/extra manifests, hardcoded to `targetRevision: master`, so a "pinned" prod would still have every child quietly re-resolving its secrets against master's current tip at every sync. Fixed with `scripts/pin-release.sh` (new release step 4, `CLAUDE.md` Releases section): rewrites those self-references to the exact tag, deliberately on a commit that moves the tag but is **never merged into `master`** — `master`'s own copies must stay floating on `master` for dev, only the tag itself needs the frozen references. Applied immediately to a real overdue release: `v0.1.3` cut (Phase 3 close, all of Phase 3.5, today's mini PC bootstrap — hadn't been tagged since `v0.1.2` on 2026-08-09), pinned, and the mini PC's `root` switched to track it — validated live (child `Application` sources confirmed pinned via `kubectl`, Nextcloud still answering `200` after the switch). `nextcloud`/`root`'s temporary `syncPolicy: null` (from testing the Nextcloud fix pre-merge) was re-enabled once merged, no drift.
- **WireGuard reinstalled on the mini PC itself, dev-machine instance retired (2026-08-20)**: the 2026-08-15 validation had run on the dev machine by necessity (mini PC didn't exist yet) — `scripts/wireguard-setup.sh`'s bootstrap-vs-add-peer branching keys off whether `wireguard/wg0.conf.sops.yaml` already exists in the repo, so running it as-is on the mini PC would have tried to add a peer to the *dev machine's* server identity instead of giving the mini PC its own. Fixed by cloning the repo fresh on the mini PC, deleting that file from the local checkout only (not the committed history) so the script bootstrapped a genuinely new server identity there, then committing the mini PC's resulting `wireguard/wg0.conf.sops.yaml` back from the dev machine (which has the signing key) in place of the old one. **Validated**: `wg-quick@wg0` active on the mini PC, config round-trip-verified via `sops -d`. The dev machine's own test instance is being decommissioned now that the mini PC is the real one — matches the architecture's intent (this VPN gates access to *the cluster*, which now lives on the mini PC, not the dev machine).

- **Real domain + Let's Encrypt — pipeline built and validated on a canary (2026-08-22)**: `offsystem.fr` (Gandi) wired to Traefik's native ACME resolver via DNS-01 (`gandiv5` provider, bundled in Traefik's own `lego` — `cert-manager` deliberately rejected, no native Gandi support there, only unmaintained-looking community webhooks). One wildcard cert (`*.offsystem.fr` + apex) issued and reused by every future service, triggered by a single `router.tls.domains` annotation on a plain `Ingress` (no `IngressRoute` CRD needed). New `gitops/bootstrap/traefik-acme-helmchartconfig.yaml` (`HelmChartConfig`, overlays k3s's own built-in Traefik `HelmChart` — never edit that one directly). Validated staging-CA-first, then switched to production (real gotcha: Traefik doesn't auto-renew just because `caServer` changed — had to delete `acme.json` and restart) — confirmed via `openssl s_client` **and** a genuinely external fetch (not from the LAN): real "Let's Encrypt" issuer, real browser padlock from a phone on 4G. **Uptime Kuma picked as the canary** (public status page, no SSO/redirect-URI coupling to break) — additive only, `myown-uptime.local` untouched and reverified working; a separate raw `Ingress` was needed since the chart's own `ingress.tls` template hardcodes a `secretName`, incompatible with a Traefik-managed ACME cert. New `gitops/apps/gandi-dyndns.yaml` (CronJob every 5 min, keeps `@`/`*` DNS records pointed at the current public IP — Free gives no static-IP option here, confirmed via the Freebox UI) hit two real bugs on first manual test: no IPv6 egress from the regular pod network despite the host having native IPv6 (same class of fix as LiveKit's `podHostNetwork` — `hostNetwork: true` here too), and a `set -eu` + `[ cond ] && cmd` pattern that silently aborted the whole script instead of just skipping the AAAA step. **Generalized to the 8 other family-facing services same-day (2026-08-22)**, once the canary was confirmed solid: Vaultwarden, Nextcloud, Immich, Jellyfin, Authentik, Tuwunel, LiveKit, lk-jwt-service each got their own additive public `Ingress`/subdomain, all reusing the same wildcard cert (confirmed via `openssl s_client` on all 9 hosts — one certificate, no new ACME requests). **Grafana deliberately excluded** — already documented admin-only in `roadmap.md`, exposing it would contradict the project's own no-admin-tools-on-the-internet principle. Real `400` found on `nextcloud.offsystem.fr` (Nextcloud rejects unrecognized `Host` headers via `trustedDomains`, independent of the cert/Ingress being correct) — fixed by adding the new hostname to `gitops/apps/nextcloud.yaml`. **Self-inflicted incident while testing that fix live**: `occ config:system:set` run via `kubectl exec` (root, not `www-data` — the same class of bug already hit during the 2026-08-20 bootstrap) overwrote an existing `trustedDomains` index instead of adding a new one, and re-broke `config.php`'s ownership, taking Nextcloud down (`503`) for the whole family briefly — caught and fixed within the same session (`chown` + correct index). DNS+cert reachability only for this pass — full external SSO login (Vaultwarden/Nextcloud/Immich/Tuwunel/Jellyfin still reference `myown-*.local` in their own SSO config) and LiveKit's echoed `LIVEKIT_URL` are real follow-up work, deliberately not attempted yet. Full write-up in `notes-techniques.md`, new install step in `manuel-installation.md` §16.

- **SSO migrated to the public hostnames for all 5 integrated services (2026-08-22)**: closes the gap left by the domain/cert work above — pages loaded publicly, but login didn't complete end-to-end until this. DNS split-horizon extended to `*.offsystem.fr` (`gitops/bootstrap/coredns-custom.yaml`, same mechanism as `myown-*.local`) so server-to-server OIDC calls never leave the cluster. Each service's `authority`/`discoveryuri`/`issuer_url` switched to `authentik.offsystem.fr` — required, not cosmetic: Authentik mirrors the Host of whichever request fetches discovery into the browser login redirect too, so the old LAN-only hostname there would silently break external login regardless of the service's own reachability. **Tuwunel's `server_name` deliberately untouched** (Matrix identity baked into every user ID — changing it after real accounts exist corrupts them irrecoverably, confirmed via Matrix docs/community); only `issuer_url`/`callback_url` changed. Three more real bugs found validating each service for real (not just curl): Vaultwarden's `sso_users` table links identity by `(issuer, sub)`, so the authority change orphaned the real already-linked account (`existing SSO user with same email`, `400`) — fixed with the user's explicit go-ahead (Restic backup first, clean StatefulSet stop, direct SQLite `DELETE` via a disposable pod on the same PVC, restart, re-verified with a real login). Jellyfin's plugin builds its redirect URI from the raw inbound scheme, ignoring `X-Forwarded-Proto` entirely (confirmed dead end: explicit header from a pod already inside `Known Proxies` had zero effect) — the actual fix was the plugin's own `ForceHttpsRedirect` setting, previously hardcoded to `false` in `scripts/jellyfin-sso-setup.py`. **Real incident hit re-enabling `syncPolicy` after the canary**: the mini PC's `root` still tracks tag `v0.1.4` (see dev/prod entry below) — selfHeal immediately reverted Vaultwarden's ConfigMap and all 5 Authentik blueprints' new `redirect_uris` back to LAN-only, silently, with ArgoCD still showing `Synced`/`Healthy` throughout. `syncPolicy` disabled again immediately, every fix reapplied and reverified. **Never re-enable `selfHeal` on the mini PC without cutting a release and rerunning `scripts/pin-release.sh` first** — the already-known "selfHeal reverts an uncommitted live test" friction applies at full-chantier scale, not just one file, whenever the tracked tag is behind. Full write-up in `notes-techniques.md`.

- **Account-recovery Authentik flow built and validated (2026-08-22)**: last Phase 3.5 installer-design item. Creating a second real user (Katia) via the Authentik admin UI itself (not `ak shell`, deliberately — mirrors the real family-onboarding flow) surfaced a real gap: no `Flow` with `designation="recovery"` existed, so the UI only offered to set the password directly rather than generate a shareable recovery link (zero-knowledge model broken). New minimal blueprint (`gitops/secrets/authentik-blueprints/account-recovery.sops.yaml`) reuses the existing password-change `Stage`s (`default-password-change-prompt`/`-write`), bound to a new `recovery`-designated `Flow`, wired via `Brand.flow_recovery`. **Validated end-to-end**: Katia's account created and her password set entirely via a real recovery link, no admin ever seeing or choosing it; she logged in successfully afterward.
- **LiveKit branched externally, TURN added, real multi-device call test attempted — infra confirmed correct, one client-side limitation left open (2026-08-22)**: last Phase 3 exit-criteria item, reframed by the user's own observation that testing externally (now that the domain exists) was more meaningful than the originally-planned LAN test. `well_known.livekit_url`/`LIVEKIT_URL` switched to `*.offsystem.fr`; a real bug found only via an actual Element client (curl didn't catch it) — `well_known.client` also needed to move off `myown-tuwunel.local`, distinct from `well_known.server` (federation, deliberately untouched). **No TURN server had ever been configured** — real calls between two accounts worked from a PC but failed on every mobile client (`removing participant without connection` in `livekit-sfu` logs, ~2-4s after start — mobile WebRTC stacks depend on UDP with no usable fallback once direct fails). Fixed: `turn.enabled: true` (TCP/TLS on 5349, chart-native `turn.secretName`), reusing Traefik's own Let's Encrypt wildcard cert rather than issuing a second one — new `livekit-turn-cert-sync` CronJob (`gitops/apps/livekit-turn-cert-sync.yaml`) extracts it daily straight from Traefik's `acme.json` via a namesake-scoped cross-namespace RBAC grant. Two more real bugs found chasing this down: LiveKit's own external-IP *self-ping validation* silently fails on this Freebox (UDP hairpin NAT unsupported, unlike the already-proven TCP hairpin) and falls back to advertising unroutable internal IPs instead of the correctly-STUN-discovered public one — fixed with `rtc.skip_external_ip_validation: true`, a flag the chart's own values.yaml documents for exactly this NAT class; and a `RollingUpdate`/`hostNetwork`/single-node deadlock on any future rollout of this Deployment (surge pod can never schedule, port conflict) — fixed with `deploymentStrategy: {type: Recreate}`. **Both fixes validated live via `livekit-sfu`'s own startup logs** (public IP correctly present in the advertised candidate list; rollout completing instead of hanging). **Not resolved**: calls still fail with `MISSING_MATRIX_RTC_TRANSPORT` on mobile (Element X app and mobile browser alike) despite all of the above — a long elimination process (cache, `server_name` routability, CORS, cert chain, all individually tested and ruled out) landed on hard proof, via Traefik access logs enabled specifically for this, that the server responds correctly (`200`, correct payload) and the failure happens client-side in Element Call's own JS, after a valid response — matches an external GitHub issue with the identical error code, though not conclusively resolved for our setup. Documented as a known limitation rather than chased further blind (no access to the failing browser's own console). Full elimination trail in `notes-techniques.md`.
- **Real live-state discrepancy found and checked, no damage done**: `authentik`'s `syncPolicy.automated.selfHeal` was found still `true`, unlike `root`/`livekit`/`tuwunel` (believed disabled together). Checked rather than assumed safe: the mini PC's pinned tag (`v0.1.5`) already contains the correct SSO content selfHeal would reassert, so no actual reversion occurred — but it means `authentik` needs aligning with the other three (disabled) at the next commit/release/re-pin cycle, not left as an inconsistency.
- **Everything above committed, released, re-pinned (2026-08-22)**: PR #107 merged; deploying it for real via ArgoCD surfaced one more genuine bug the earlier hand-patched live test never exercised — the LiveKit chart's `turnLoadbalancer.enable` defaults to `true`, silently creating a second `LoadBalancer` `Service` on port 443 that collided with Traefik's own. Fixed (`turnLoadbalancer.enable: false`, PR #109), which meant a second release. `v0.1.6` then `v0.1.7` cut, `scripts/pin-release.sh` rerun, mini PC's `root` re-pinned to `v0.1.7` and resynced — all 13 Applications confirmed `Synced`/`Healthy`, no crashed pods, all 7 public hostnames responding correctly. `syncPolicy` naturally back to normal on every child Application as a side effect of `root`'s resync (each one's own manifest already declares `automated: {selfHeal: true}` — the earlier live-only `syncPolicy: null` overrides were never persisted in git, by design). **`root` itself deliberately stays on manual sync** — not an oversight, matches the 2026-08-15 decision that sensitive-service syncs go out announced/manual rather than automatic.
- **Phase 4 priorities reordered by the user (2026-08-22)**: Pi-hole moved to last — not a priority. Immediate focus instead: the auto-remediation watchdog (done, see below), Android/federation validation (next, "tomorrow"), then Mailcow ("enfin !" — explicit impatience for this one). Saved to memory (`project_phase4_priorities.md`) since `roadmap.md`'s own Phase 4 list wasn't reordered, only this working priority.
- **Auto-remediation watchdog — built and validated end-to-end with a real forced outage (2026-08-22/23)**: first Phase 4 item tackled, attacked directly (no canary) per the reordered priorities above. Host-level systemd (`myown-watchdog.timer` + `.service`, `scripts/watchdog-setup.sh`/`watchdog-check.sh`) — deliberately outside GitOps, same circular-dependency reasoning as WireGuard (`architecture.md` §6): checks `k3s kubectl get --raw /healthz` every 60s, restarts `k3s.service` automatically after 3 consecutive failures (motivated by the 2026-08-20 incident where only a full service restart fixed an identically-reproducing crash loop), capped at 3 remediations/hour to avoid masking a real unfixable problem with an infinite restart loop. **Alerting added on the user's own request** (not originally planned): notifies the existing `#etat-du-systeme` Tuwunel room on remediation attempts and on giving up — honest limitation stated upfront (Tuwunel runs inside the cluster being watched, so delivery is only possible once it's reachable again; queued locally and flushed on the first healthy tick, same blind spot already accepted for Uptime Kuma). **Three real bugs found running an actual forced-failure test** (`sudo systemctl stop k3s`), none caught by reviewing the script itself — all the same class: an assumption that holds in the interactive install shell (`robin` user) but not in the systemd runtime (root, different `PATH`/`$HOME`): sops not found (root's `PATH` lacks `~/.local/bin`), wrong repo root (derived from the installed copy's own location in `/usr/local/bin/` instead of the real checkout), and no age key at root's default path. All three fixed the same way — resolved once in the interactive install script, baked into the systemd unit via `Environment=` (`MYOWN_WD_SOPS_BIN`, `MYOWN_WD_REPO_ROOT`, `SOPS_AGE_KEY_FILE`) — across PRs #111-#114. **Fully validated end-to-end, each step independently confirmed**: 3 consecutive failures logged, `k3s.service` auto-restarted, cluster confirmed `Ready` with zero manual intervention, and the Tuwunel notification confirmed delivered via a direct Matrix API read (not just assumed from a non-erroring send). Real bonus fix along the way: the mini PC's own git checkout turned out to be many releases behind (never pulled since before 2026-08-20), with `wireguard/wg0.conf.sops.yaml` showing as "locally modified" — investigated via public-key comparison before touching anything, confirmed the local file matched the *active* WireGuard interface and was identical to what `master` already had committed (just via a stale local `HEAD`, not a real divergence) — resolved with `git stash && git pull && git stash pop`, zero data loss. Full write-up in `notes-techniques.md`.

- **Tuwunel `server_name` migrated to `offsystem.fr`, real federation validated end-to-end (2026-08-23)**: closes the roadmap Phase 3 federation item, deferred since it was originally scoped. `myown-tuwunel.local` made real federation structurally impossible (`.local` is DNS-root-blocked, not just misconfigured) — checked first (per the user's explicit ask) whether any `server_name` migration path exists: none does, anywhere (no Matrix Spec Change proposal, Synapse refuses to boot on a change, Tuwunel/Continuwuity's own docs say a database wipe is required). User's call, made deliberately while the cost was still low (2 real accounts): reset now rather than later. New identity is the apex domain `offsystem.fr` (not `tuwunel.offsystem.fr` — recommended by Tuwunel/Continuwuity/Dendrite docs themselves, keeps user IDs clean: `@robin:offsystem.fr`), decoupled from the unchanged hosting subdomain via `.well-known/matrix/server` delegation (new minimal `Ingress`, `gitops/manifests/tuwunel-federation-delegation/`) — validated risk-free before touching any data. Real backup taken, full data-PVC wipe (no partial migration is possible), fresh `tuwunel.toml`, both real accounts (`@robin`, `@kate`) and the alert bot re-registered via the same SSO/script flows as originally. **Real bug hit mid-operation**: `selfHeal` (left enabled on `tuwunel`, forgotten) reverted the `Deployment` scale-down instantly, and completed Restic-backup pods holding a stale reference to the PVC blocked its deletion even after being `Completed` — both diagnosed and fixed live. **Unexpected bonus, confirmed by the user**: last night's unresolved `MISSING_MATRIX_RTC_TRANSPORT` limitation disappeared entirely after this migration, with zero LiveKit-side changes — the user's original hunch ("the `.local` name must be breaking something"), set aside in favor of a long client-side investigation, turns out to have been correct all along. **Federation validated in layers, each one independently proven**: matrix.org's own official federation tester confirms `AllChecksOK: true` over IPv4 (real third-party validation, cert/keys/delegation all correct) — found and deliberately deferred a second, separate issue here too (Traefik's `Service` is IPv4-only at the Kubernetes level, `ipFamilies: ["IPv4"]`, a cluster-wide dual-stack gap, not a quick fix); then a real cross-server test with an external matrix.org account, which found a genuine bug — inbound federation (matrix.org reaching us) worked immediately, but outbound joins (us reaching matrix.org) failed repeatedly. Root cause confirmed directly with a debug pod: regular pod networking has no IPv6 egress route at all (same underlying gap already hit once for `gandi-dyndns`), and outbound federation requests were trying matrix.org's AAAA record with no IPv4 fallback. Fixed with `hostNetwork: true` + `dnsPolicy: ClusterFirstWithHostNet` on the Tuwunel `Deployment` (same pattern as LiveKit) — **confirmed by a full successful join in the server logs** (`make_join`/`send_join`/notary key exchange/join event, not just "it worked on screen"). Full write-up in `notes-techniques.md`.

- **v0.1.9 released, mini PC re-pinned, all Applications healthy (2026-08-23)**: closes out the federation migration above — everything committed, released, `scripts/pin-release.sh` rerun, `root` re-synced to `v0.1.9`, all 13 Applications confirmed `Synced`/`Healthy`, no crashed pods anywhere. `tuwunel`'s `syncPolicy` naturally back to normal (same pattern as the watchdog release: the live-only `syncPolicy: null` override was never persisted in git, so `root`'s own resync restored it — safe here since the pinned tag already matches what's live). **Katia still needs to redo her Element/Tuwunel login** (Authentik side untouched by the migration, just the Matrix account needs a fresh SSO login under the new identity).
- **Mailcow deployment research done, real architectural constraint found — decision needed before implementation starts (2026-08-23)**: prep work before switching to a fresh context, same spirit as the 2026-08-13 Phase 3.5 planning session (research only, nothing implemented). **Verdict: Kubernetes is not viable for Mailcow, confirmed not just assumed** — no Helm chart exists anywhere, mailcow's own maintainers have explicitly refused k8s support (GitHub issue #189: *"it will not be used by mailcow"*; #7200, still open: *"you can't just spin up existing containers as-is in k8s"*), and real blockers exist in the compose stack itself: `netfilter-mailcow` runs `privileged: true` + `network_mode: host` manipulating the host's own iptables directly (real collision risk with k3s's own kube-proxy/flannel rules if co-located), and `dockerapi-mailcow` drives the Docker socket directly for its own admin UI — no Kubernetes equivalent at all, the real reason this isn't just a packaging inconvenience. Universal recommendation found: native docker-compose, outside k3s entirely (official Ansible role exists: `mailcow/mailcow-ansiblerole`). **Open decision for the next session, not yet made**: run it on the mini PC itself (alongside k3s, not inside it — same category as WireGuard/the watchdog, but the iptables collision risk here needs real testing, not assumption) vs. a separate dedicated machine (clean isolation, but a second piece of hardware, in tension with the "one mini PC" principle from `architecture.md`). Full write-up, including the port-80/443-vs-Traefik wrinkle, in `notes-techniques.md`.

- **Pivoted from Mailcow to Mailu after a real feature/architecture comparison (2026-08-23)**: the Mailcow k8s-infeasibility research above (same day) prompted a direct comparison against Mailu, a Kubernetes-native alternative, before accepting the "docker-compose outside k3s" workaround Mailcow would have required. **Decisive factor: Mailu has a Helm chart maintained by the Mailu org itself** (`github.com/Mailu/helm-charts`, chart `2.7.3`, pushed the same day this research happened — not stale), and its compose stack (checked directly, `setup/flavors/compose/docker-compose.yml`) has **no `privileged`, no `network_mode: host`, no Docker-socket mount** anywhere — none of Mailcow's structural blockers exist, so Mailu fits the existing ArgoCD/`Application` pattern with no hosting-mechanism decision needed at all. Real feature tradeoffs found, not a clean win either way: SOGo (Mailcow's webmail) has native Gmail-style multi-tag labels Roundcube (Mailu's) lacks by default (Roundcube core issue #4986, still open, no bundled tag plugin); but Mailu's default-bundled `rcmcarddav` Roundcube plugin can point at an external CardDAV source, including Nextcloud specifically (documented pattern), for contacts autocomplete in the compose window — SOGo cannot do this at all (`SOGoUserSources` only accepts LDAP or SQL). Both suites tie on aliases/catch-all (SQL `LIKE` syntax in Mailu's admin UI) and Sieve filters (UI-based by default on both, not raw script). Calendar-in-webmail judged unnecessary either way — Nextcloud Calendar already covers it; Mailu's optional Radicale component will stay disabled (`WEBDAV=none`), unlike Mailcow where SOGo's calendar ships bundled and unused rather than absent. **Two real caveats flagged before starting implementation, not yet resolved**: the chart's README lists `cert-manager` as a prerequisite, which this project deliberately doesn't use (Traefik's native Gandi DNS-01 ACME instead) — needs checking whether the chart can consume an existing TLS secret instead; and mail ports are exposed via `front.hostPort.enabled: true` by default (hostPort binding, not full `hostNetwork`) — compatibility with Traefik already holding 80/443 via k3s's ServiceLB needs designing, not assumed. Docs updated same day: `architecture.md` §5.6, `roadmap.md` Phase 4, this file's design-philosophy list — all now read "Mailu". Full research write-up in `notes-techniques.md`.

- **Mailu deployed on the dev cluster and validated end-to-end, SSO included (2026-08-23)**: official Helm chart, base deployment + Authentik ForwardAuth SSO built and proven in the same session (staged: mechanism proven in isolation first against a throwaway `traefik/whoami` target, then native login + a real internal mail flow validated before SSO was switched on — see `notes-techniques.md` for the full blow-by-blow). Real bugs found and fixed at every stage, each confirmed live rather than assumed: an unrelated Traefik ACME outage discovered on the dev cluster along the way (`HelmChartConfig`/Gandi secret both missing, certs held in memory only — any pod restart would have broken HTTPS cluster-wide; repaired); `front` needs a real mounted cert independent of any Ingress (own STARTTLS/HTTPS termination) — now synced via a new `mailu-cert-sync` CronJob, same pattern as `livekit-turn-cert-sync`; Traefik verifies backend TLS certs by default once told to speak HTTPS to a backend, which broke against the pod IP — fixed with a `ServersTransport` (`insecureSkipVerify`); front's raw mail ports are `hostPort`-bound by default with no `deploymentStrategy` override in this chart, deadlocking any future rollout on this single-node cluster — `hostPort` disabled (nothing needs it yet, real external mail ports deferred to the VPS-relay design); the embedded Authentik outpost had no `authentik_host_browser` set, redirecting logins to `http://localhost` (new idempotent `scripts/authentik-outpost-config.sh`); a `ProxyProvider` created outside Authentik's own admin API doesn't auto-populate `redirect_uris`/`grant_types` (set explicitly in the blueprint); the OAuth callback path needs a cross-namespace route to Authentik that a plain Ingress can't express (new `IngressRoute`, without which SSO silently fails back to Mailu's native login with no visible error); and `mailu-admin`'s pod needed a manual restart to pick up an already-updated shared ConfigMap. **Confirmed working via a real browser login**: Authentik login → straight into Roundcube webmail, no native Mailu form. Not yet done: Restic backup, `rcmcarddav` → Nextcloud contacts, and everything past this in Phase 4 (see below) — deliberately out of scope for this pass.

- **VPS façade discovered already provisioned and hardened — in a different, untracked session (2026-08-23)**: asked to "provision the VPS façade," found nothing in this repo (roadmap/notes-techniques both still said "not yet done") — but the work was real, just done in a separate Claude Code session that never wrote its outcome back to this file. Recovered via the session-management tools (full-text search across other session transcripts, then `list_events` to read the actual transcript) rather than trusting the user's memory blindly or my own doc search alone — then independently confirmed with a real SSH connection before believing any of it. **Real state, now documented in `notes-techniques.md`**: OVH VPS-1 (`vps-20d1e001.vps.ovh.net`, `51.178.46.161`), chosen over Hetzner (whose cheap tier was out of stock at the time) partly because OVH ships port 25 open by default. Hardening fully done and reverified live: key-only SSH (password auth disabled — hit a real gotcha doing it, two conflicting `sshd` drop-in configs where cloud-init's silently won by alphabetical load order), `ufw` (SSH-only), `unattended-upgrades`. **Lesson applied going forward**: any session touching a resource outside this git repo (a VPS, a third-party account, ...) needs to write the resulting state back here before ending, not just commit code — this file's whole point is to make that unnecessary to reconstruct.

- **VPS mail relay — Postfix, DNS, tunnel, validated in both directions with real external mail (2026-08-24)**: closes the "Postfix relay + SPF/DKIM/DMARC" next step above. Dedicated WireGuard tunnel (`wireguard-mail-relay/`, separate subnet/port/keys from the admin VPN — a compromised internet-facing VPS should only ever reach Mailu's SMTP port, never `kubectl`/SSH) bootstraps the VPS as server, home (dev machine today) as client. **Two real bugs hit setting up the tunnel itself**, neither in the script: it silently regenerated keys over an already-working tunnel from earlier in the session (context compaction lost that state — a stray `wg-quick down` outside systemd also left systemd's `active (exited)` state stale, making a subsequent `start` a silent no-op) — both resolved by cleanly resyncing both ends rather than forensically recovering old keys, since nothing depended on the tunnel yet. Mailu:25 exposed to the tunnel via a persistent `kubectl port-forward` systemd service (`scripts/mailu-portforward-setup.sh`) rather than reactivating Mailu's `hostPort` — that would need a third dev-cluster recreation (new k3d port mapping) for a cluster getting abandoned once Mailu migrates to the mini PC anyway. Postfix on the VPS (`scripts/postfix-relay-vps-setup.sh`) is a dumb relay only — no DKIM signing there, Rspamd already signs on the Mailu side. DNS at Gandi (A/MX/SPF/DKIM/DMARC, `scripts/gandi-mail-dns-setup.sh`) and the OVH PTR record both applied — DKIM's public key was extracted from Mailu's own private key file using Mailu's own `strip_key()` function (never printed the private key). **Claude Code correctly blocked from decrypting the Gandi PAT and from reading the WireGuard private key** by the auto-mode classifier — both handed off as ready-to-run scripts for the user to execute themselves. **Real deliverability bug found on the first live outbound send**: the VPS's native IPv6 got preferred for the connection to Gmail, but SPF/PTR here are deliberately IPv4-only, so Gmail bounced on SPF failure (confirmed in the bounce log, not guessed) — fixed with `smtp_address_preference = ipv4`. **Fully validated in both directions with real accounts**: a real Gmail send landed in Mailu's actual Dovecot maildir (not just a queued status), and a real Mailu send landed in the Gmail **inbox**, not spam. Also found a live ArgoCD gap while testing Mailu's new `externalRelay` config pre-merge: `gitops/bootstrap/root-app.yaml` still has `automated: {selfHeal: true}` hardcoded, contradicting the 2026-08-15 manual-sync decision — since that file lives in the GitOps-exempt bootstrap layer, it was likely never corrected after that decision was made, and a previous session's live-only `syncPolicy: null` override never persisted. Not fixed in this session (out of scope for the mail chantier) — flagged for a future pass, same as the earlier `authentik` selfHeal discrepancy.

- **Two pre-existing, unrelated crashes on the dev cluster found and fixed (2026-08-24)**: found while merging PR #126 above — `root`'s selfHeal (see discrepancy noted in that entry) got re-enabled to test the merge, which cascaded into `mailu`'s own selfHeal reasserting itself and surfaced two outages that had been silently sitting there, neither caused by this session's own work. **`livekit-sfu`**: ArgoCD sync had been failing for days (`spec.strategy.rollingUpdate: Forbidden: may not be specified when strategy type is 'Recreate', retried 5 times`) — a stale `rollingUpdate` sub-field left over from before the `Recreate` fix (see the 2026-08-22 LiveKit entry) blocked every subsequent strategic-merge-patch attempt, silently leaving the pod running a template from before that fix ever landed (no TURN cert mount, hence `TURN tls cert required: open : no such file or directory`). Fixed with a one-time JSON patch clearing the stale field, then a manual `argocd app sync` (`--core` mode, direct k8s API, no login needed) to force the actual resync — confirmed live via the pod's own startup log (`Starting TURN server`, correct cert mount, `1.13.5`). **`tuwunel`**: exactly the user's own hunch — the dev cluster's Tuwunel database still carried the pre-migration identity (`myown-tuwunel.local`), never wiped when `server_name` moved to `offsystem.fr` on 2026-08-23 (that reset was only ever performed on the real install, replaying it here had been missed) — `Database belongs to myown-tuwunel.local; configured server name is offsystem.fr. Cannot reuse.` (Tuwunel's own anti-corruption check). Fixed by replaying the same recipe already used for the real migration: a manual Restic backup triggered first (confirmed via `restic snapshots`, a real new snapshot with correct file counts), then the dev-only `tuwunel-data` PVC wiped via a disposable pod (blocked once for Claude Code by the auto-mode classifier on the `rm -rf` pattern — handed to the user to run themselves, same boundary as secret decryption), then Tuwunel restarted and confirmed initializing a fresh database with the correct `server_name`. **Both fixes are dev-cluster-only** — no kubectl context to the mini PC exists on this machine, and the PVCs/Deployments touched are dev's own, structurally isolated from the mini PC's storage. **A third issue found but deliberately left alone, on the user's explicit call**: `jellyfin` is `Degraded` on dev too, but for an unrelated, older reason (stuck since ~2026-08-20, not "yesterday") — its current desired revision (5) references a `hostPath` media volume at `.../nextcloud-data/data/__groupfolders/1`, a Nextcloud "group folder" that exists on whichever Nextcloud the mini PC's admin created it on, but was never replicated on dev's own separate Nextcloud instance (group folders are an admin-UI action, not GitOps-managed, so of course they don't match across separate Nextcloud instances). Kubernetes correctly keeps the previous revision's pod alive since the new one can never become ready (real Jellyfin usage on dev is unaffected — only the ArgoCD health badge is misleading), but the *fix* would touch `gitops/apps/jellyfin.yaml`, shared verbatim between dev and the mini PC — deliberately not touched, real risk of breaking a mount that works fine in production, for a dev-only cosmetic status. **Left as a known, tracked gap**: revisit either by creating a matching group folder in dev's own Nextcloud, or by finding a way to make this path per-cluster rather than shared, next time dev-cluster fidelity actually matters (e.g., before using dev to validate a future Jellyfin change).

- **Mailu Restic backup — done and validated (2026-08-24)**: closes the last known backup gap for Mailu, same day as the two dev-cluster outage fixes above. No separate database here — `persistence.single_pvc: true` puts everything (admin's SQLite db, DKIM private key, Dovecot maildirs, rspamd state, webmail config) on one PVC, backed up in one pass, same shape as Vaultwarden/Tuwunel (`gitops/manifests/mailu-backup/`, `RESTIC_PASSWORD` added to the existing `mailu-secrets` via `sops --set`, scheduled 04:30). **The same stale-`rollingUpdate`-blocking-`Recreate` bug from the LiveKit entry above recurred a second time**, this time on `mailu-postfix`, blocking this PR's otherwise-unrelated sync — re-patched the same way, but flagged as a real recurring risk rather than a one-off: `syncOptions: [Replace=true]` on that resource would eliminate the whole bug class instead of re-fixing it each time it resurfaces. **Also discovered while chasing why the new CronJob/PVC weren't appearing after an apparently-successful `argocd app sync mailu`**: `root` (the app-of-apps), not `mailu` itself, owns and refreshes the `mailu` Application's own `spec` (including its `sources` list) — syncing `mailu` alone only reconciles against whatever `spec` it already has in memory, which can be stale. Correct order, confirmed live: sync `root` first, then `mailu`. **Validated with a real backup + restore cycle**: manual backup produced a real 85.266 MiB snapshot (not empty); a first restore attempt failed (`/repo` mounted read-only — `restic restore` needs to write a lock file, `Save(<lock/...>) returned error ... read-only file system` in an infinite retry loop) — fixed by mounting `/repo` read-write (only `/data`, the source, needs to stay read-only); restored content confirmed by name: `admin/main.db`, `dkim/offsystem.fr.dkim.key`, and the exact test emails sent/received earlier in this same session, found under their real filenames in `dovecotmail/admin@offsystem.fr/new/`.

**Next step**: fix `gitops/bootstrap/root-app.yaml`'s `automated: {selfHeal: true}` to actually match the documented manual-sync decision (see entry above — re-confirmed still live today, twice now caused real friction rather than just being a theoretical inconsistency). `rcmcarddav`→Nextcloud contacts integration, migrating Mailu + the relay tunnel to the mini PC, progressive correspondent migration, and a sustained deliverability monitoring period per the roadmap's own exit criteria. Two IPv6 gaps deliberately deferred as a future chantier (Kubernetes `Service` dual-stack for inbound; a structural, not per-service, fix for pod IPv6 egress outbound — `gandi-dyndns`, LiveKit, and Tuwunel each worked around the same underlying issue individually via `hostNetwork`, and this relay's outbound SMTP now also works around it via `smtp_address_preference`). The dev-cluster Jellyfin group-folder gap above is tracked but deliberately unfixed. Pi-hole stays deliberately last per the user's stated priority order. `scripts/install.sh` still only automates the k3d (dev) path, not yet the k3s bare-metal one used on 2026-08-20 — see `roadmap.md`.

## Repository conventions (decided, not yet all implemented)

- **Structure**: monorepo — docs, GitOps manifests (once they exist, ArgoCD will watch this repo directly), and any custom integration code all live here. No plan to split repos at this stage.
- **License**: AGPL-3.0 (`LICENSE`) — network-copyleft, chosen deliberately so a third party can't wrap this project into a closed SaaS.
- **Git platform**: GitHub.
- **Secrets**: SOPS + age, decrypted automatically at ArgoCD sync time via KSOPS (chosen over manual `sops -d | kubectl apply` to keep disaster recovery fully automatic once the age key is restored — see `docs/vision-long-terme.md`-style reasoning: worth the extra setup cost for a project meant to be reproducible by others). `.sops.yaml` holds the real public key — never commit an unencrypted secret or the age private key.
  - Private key lives at `~/.config/sops/age/keys.txt` (sops' default lookup path, no env var needed) — **back this up somewhere durable**. Losing it is only harmless while zero secrets are encrypted; past that point it means every encrypted secret in the repo becomes permanently unrecoverable and has to be rotated.
  - New secret files: name them `*.sops.yaml` (matches `.sops.yaml`'s `path_regex`), write plaintext under `stringData`, then `sops --encrypt --in-place path/to/name.sops.yaml`. Never hand-edit an already-encrypted file.
  - Each secret lives in `gitops/secrets/<name>/` as a small kustomize app (`kustomization.yaml` + `secret-generator.yaml` ksops generator + the `*.sops.yaml`), wired into the owning service's Application as a second `sources` entry (multi-source Application) — see `gitops/apps/authentik.yaml` for the pattern to copy.
  - KSOPS itself is bootstrapped onto `argocd-repo-server` the same way ArgoCD is bootstrapped: not GitOps-managed. On a fresh cluster, after installing ArgoCD:
    1. `kubectl create secret generic sops-age -n argocd --from-file=keys.txt=$HOME/.config/sops/age/keys.txt`
    2. `kubectl patch configmap argocd-cm -n argocd --type merge -p '{"data":{"kustomize.buildOptions":"--enable-alpha-plugins --enable-exec"}}'`
    3. `kubectl patch deployment argocd-repo-server -n argocd --type strategic --patch-file gitops/bootstrap/argocd-repo-server-ksops-patch.yaml`
- **CI**: `.github/workflows/lint.yml` lints Markdown and lints every commit in a PR (commitlint). Expect this to grow (Helm lint, Trivy CVE scan) once Phase 0 introduces actual manifests — don't pre-build CI stages for infra that doesn't exist yet.
- **Language**: project docs and communication are in French.
- **`package.json` / `node_modules`**: not a JS project — this Node tooling exists solely to run commit-and-tag-version, husky and commitlint. Don't treat its presence as an invitation to scaffold a JS/TS app here.
- **Test locally before pushing, not after**: run `npm run lint:md` before opening/updating a PR — don't rely on CI to discover a lint error that a 5-second local command would have caught. Commit message format is already covered by the local `commit-msg` hook. No self-hosted runner: this repo is public, and GitHub explicitly warns against self-hosted runners on public repos (a fork's PR could run arbitrary code on the runner's host) — public repos get free/unlimited Actions minutes on hosted runners anyway, so there's no capacity reason to reach for one.

## Git workflow & releases

Trunk-based, single long-lived branch (`master`), no `develop`/`release` branches.

- **Dev branches**: short-lived, one per feature/fix. Before merging, clean up history with interactive rebase (`git rebase -i`) so every commit that lands on `master` is itself a valid, atomic Conventional Commit — commits are preserved through the merge, not squashed. Merge PRs with **"Create a merge commit"**, not "Rebase and merge": GitHub can only auto-sign commits it authors itself (a merge/squash commit), and rejects rebase-merge outright once signed commits are required (`gh pr merge --merge`).
- **Commit format**: [Conventional Commits](https://www.conventionalcommits.org/) (`feat:`, `fix:`, `docs:`, `refactor:`, `chore:`, `ci:`, …). Enforced by a local `commit-msg` hook (husky + commitlint, `commitlint.config.mjs` — `.mjs` is required, the commitlint GitHub Action rejects `.js`) and again in CI (`wagoid/commitlint-github-action`, checks every commit in the PR range) since local hooks can be bypassed or skipped.
- **Versioning**: SemVer (`major.minor.patch`). Stay in `0.x.y` through the POC phases (0–3 in the roadmap); reserve `1.0.0` for the MVP milestone (end of roadmap phase 3 — passwords + files/photos + messaging in real use).
- **Releases**: cut when a feature is implemented *and* validated in real use (matches the roadmap's exit-criteria philosophy — no release for a merge that only compiles/deploys but hasn't been used). Full process:
  1. Run `npm run release` (wraps `commit-and-tag-version`): bumps `package.json` version per the highest-impact commit type since the last tag, regenerates `CHANGELOG.md` from Conventional Commits (section mapping in `.versionrc.json`), creates a `chore(release): x.y.z` commit and a `vX.Y.Z` git tag.
  2. Push the tag directly (`git push origin vX.Y.Z`) — tags aren't covered by branch protection. The commit **can't** go through `git push --follow-tags` since `master` requires a PR; push it on a branch (e.g. `chore/release-x.y.z`) and merge normally like any other change.
  3. Create the GitHub Release (`gh release create vX.Y.Z --title "vX.Y.Z — Phase Name" --notes-file <file>`) — pushing the tag alone does **not** create one, they're separate GitHub objects. Title format `vX.Y.Z — <roadmap phase closed>`; body is a short French summary of what the release closes out, followed by that version's exact section from `CHANGELOG.md`. Do this for every tag going forward — `v0.1.1`/`v0.1.2` initially only existed as tags until this was noticed and backfilled.
  4. Run `scripts/pin-release.sh vX.Y.Z` **after** the release PR is merged: rewrites every `gitops/apps/*.yaml`'s own `repoURL: .../MyOwn.git` sources (secrets, extra manifests) from `targetRevision: master` to this exact tag, on one extra commit that moves the tag but is deliberately never merged into `master` — needed so a production install tracking this tag via its `root` Application doesn't have its child Applications quietly keep floating on `master` for their own secrets/manifests. `git push --force origin vX.Y.Z` to publish the moved tag. See the script's own header for the full reasoning.
- **Branch protection on `master`** is active (GitHub ruleset `master_protect`, repo Settings → Rules): PR required, `commitlint`+`markdown` checks required, no force-push, no branch deletion, **signed commits required**. Branch protection/rulesets need either a public repo or GitHub Pro on a personal (non-org) account — this repo is public specifically to get this for free.
- **Commit signing**: every commit on `master` must be SSH-signed. Anyone pushing to this repo (including on a new machine) needs their own signing key:
  1. `ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519_signing -C "commit-signing"`
  2. `gh ssh-key add ~/.ssh/id_ed25519_signing.pub --type signing` (or add it manually under GitHub Settings → SSH and GPG keys, as a *Signing Key*, not an *Authentication Key*)
  3. Locally, in the repo: `git config gpg.format ssh`, `git config user.signingkey ~/.ssh/id_ed25519_signing.pub`, `git config commit.gpgsign true` — the user should run these themselves, Claude must never modify git config.

## Design philosophy to respect when implementing

Assemble mature open-source components (Authentik, Vaultwarden, Nextcloud, Immich, Tuwunel/Element X/LiveKit, Mailu, Ollama) rather than reimplementing them. Custom development is reserved for the integration layer (SSO wiring, automations, local AI glue) — see `docs/architecture.md` §3 and §5 for the full rationale and per-component choices before proposing a different tool.
